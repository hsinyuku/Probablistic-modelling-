\ProvidesPackage{mystyle}

\usepackage[utf8]{inputenc} % Makes some special characters contained in UTF-8 directly interpretable; otherwise, we could only use ASCII-characters.
\usepackage[english]{babel} % Language of the document is in English; this is important for a number of small features.
\usepackage{csquotes} % overleaf asked me to load this package to because I loaded babel
\usepackage{dirtytalk} % this is for easier quotations, see https://www.overleaf.com/learn/latex/Typesetting_quotations; use it by putting quoted text into \say{}
\usepackage{CJKutf8} % this allows us to use chinese characters when needed (looking at you, 心瑜).
    % on how to use it: https://tex.stackexchange.com/question/223237/packages-cjk-versus-cjkutf8, http://texdoc.net/texmf-dist/doc/latex/cjk/doc/CJK.txt

\usepackage[
    backend=biber, % this specifies in which format the references are in the file that will be provided
    style = authoryear, % APA-like bibliography
    isbn = false, % never print ISBNs
    doi = false,
    url = false,
    binencoding = UTF8,
    maxcitenames = 2]{biblatex}
\usepackage{csquotes}
    
% tables
\usepackage{hyperref}

\usepackage{multirow}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable} % tables spanning multiple pages
\usepackage{tabularx}
\usepackage{makecell} % for line breaks in multicolumn-rows

\usepackage{authblk} % potentially more beautiful author affiliations

% graphs
\usepackage{tikz}
\usepackage{pgf}
\usetikzlibrary{arrows,automata}

% all packages added from previous project, don't remember what each exactly does but some of them are needed, so leave them here

\usepackage{amsmath}
\usepackage{amstext}
\usepackage{amssymb}
\usepackage{rotating}
\usepackage{titlesec}
\usepackage{fontenc}
\usepackage{caption}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{chemfig}
\usepackage{chemformula}
\usepackage{caption}
\usepackage{float}
\usepackage{hyperref}
\usepackage{multirow}

